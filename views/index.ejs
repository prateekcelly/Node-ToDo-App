<!DOCTYPE html>
<html>
  <head>
    <title>ToDo App</title>
    <link rel='stylesheet' href="https://fonts.googleapis.com/css?family=Lato:100"/>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <div class="container">
      <h2>ToDo List App</h2>
      <form action="/addtask" method="POST">
        <input type="text" name="newtask" placeholder="Add new task"/>
        <input type="submit" value="Add Task">
        <h2>Added Task</h2>
        <% for(var i=0;i<task.length;i++){%>
          <li>
            <input type="checkbox" name="check" value="<%= task[i] %>"/>
            <%= task[i] %>
          </li>
        <% } %>
        <input formaction="/removetask" type="submit" value="Remove">
      </form>
      <h2>Completed Task</h2>
      <% for(var i=0;i<complete.length;i++){%>
        <li>
          <input type="checkbox" checked /> <%= complete[i] %>
        </li>
      <% } %>
     </div>
  </body>
</html>
